{
  "operations": [
    {
      "name": "index",
      "category": "core",
      "tier": "both",
      "summary": "Parse a repo (or working tree) with tree-sitter, extract symbols/imports/call-edges/doc-comments, and persist a graph + symbol index + lexical (BM25/trigram) index. Full index on first run; incremental git-diff delta against the prior snapshot thereafter. The single ingest entrypoint every other op reads from. Ports CodeIntelligenceService.IndexRepository.",
      "inputs": "path|repo (local dir path OR hosted repo ref org/name); ref/commit (optional, defaults to working tree / default branch HEAD); base (optional commit for explicit delta); langs[] (optional filter); reindex (bool, force full vs delta); enable_vectors (bool, default false \u2014 opt-in embedding pass behind VectorStore)",
      "outputs": "{ repo_id, snapshot_id, commit_sha, indexed_files, symbols, edges, routes, delta:{added,modified,removed}, duration_ms, mode: full|delta }"
    },
    {
      "name": "search",
      "category": "core",
      "tier": "both",
      "summary": "Code-aware lexical search over the symbol/file index: BM25 (bleve) + trigram, with camelCase/snake_case tokenization so identifiers behave semantically. The default retrieval path for agents \u2014 no vectors required. Optional semantic re-rank only when vectors are explicitly enabled. Ports CodeSearchService BM25 + code_search_keyword/semantic.",
      "inputs": "query (string); repo_id (optional; defaults to current/all linked); kind (optional symbol-kind filter: function|method|class|interface|type|const); path_glob (optional); limit (default 20); mode (lexical|semantic|hybrid, default lexical \u2014 semantic/hybrid require enable_vectors)",
      "outputs": "{ results:[{symbol, kind, repo_id, path, line, signature, doc, score, snippet}], mode_used }"
    },
    {
      "name": "symbol",
      "category": "core",
      "tier": "both",
      "summary": "Full context bundle for one symbol: definition (file:line, signature, doc/rationale comment), direct callers, callees/dependencies, and covering tests. The agent's 'open this thing fully' primitive. Ports code_symbol_lookup.",
      "inputs": "symbol (exact name) OR symbol_id; repo_id (optional); include_tests (bool, default true); include_source (bool, default false)",
      "outputs": "{ symbol, kind, repo_id, path, line, signature, doc, callers[], callees[], dependencies[], tests[], source? }"
    },
    {
      "name": "callers",
      "category": "core",
      "tier": "both",
      "summary": "Who calls / invokes this symbol \u2014 inbound call edges across the graph, transitively up to a depth. Ports CodeSearchService.GetCallers / code_callers_analysis.",
      "inputs": "symbol (exact) OR symbol_id; repo_id (optional); depth (default 1); limit (default 100)",
      "outputs": "{ callers:[{symbol, repo_id, path, line, depth, edge_kind}] }"
    },
    {
      "name": "refs",
      "category": "core",
      "tier": "both",
      "summary": "All references/usages of a symbol (reads, writes, type uses, not just calls) across the indexed corpus. Ports CodeSearchService.GetReferences / code_references_analysis.",
      "inputs": "symbol (exact) OR symbol_id; repo_id (optional); ref_kind (optional: call|read|write|type|import); limit (default 200)",
      "outputs": "{ references:[{symbol, repo_id, path, line, ref_kind, snippet}] }"
    },
    {
      "name": "neighbors",
      "category": "core",
      "tier": "both",
      "summary": "Adjacent nodes of a graph node (symbol or file) along chosen edge types and direction \u2014 the local-subgraph primitive for agent graph-walking. Mirrors graphify get_neighbors but multi-repo aware.",
      "inputs": "node (symbol_id|file path|symbol name); repo_id (optional); direction (in|out|both, default both); edge_kinds[] (optional: calls|imports|references|covers|route); depth (default 1); limit (default 100)",
      "outputs": "{ node, neighbors:[{node, kind, repo_id, edge_kind, direction, depth}] }"
    },
    {
      "name": "path",
      "category": "core",
      "tier": "both",
      "summary": "Shortest dependency/call path between two graph nodes (e.g. how does the HTTP handler reach the DB write). Cross-repo capable via route-contract bridge edges. Mirrors graphify shortest_path.",
      "inputs": "from (node id|symbol|file); to (node id|symbol|file); edge_kinds[] (optional filter); max_hops (default 8); allow_cross_repo (bool, default true on hosted)",
      "outputs": "{ found, hops, path:[{node, repo_id, edge_kind}] }"
    },
    {
      "name": "impact",
      "category": "core",
      "tier": "both",
      "summary": "Single-repo blast radius for a set of changed symbols or files: transitively impacted symbols, files, and covering tests, ranked by directness. The CI/PR gate's core within-repo primitive. Ports CodeSearchService.GetImpactForChangedSymbols / code_impact_analysis.",
      "inputs": "symbols[] OR changed_paths[] OR diff (unified diff); repo_id (optional); max_depth (default 3); include_tests (bool, default true)",
      "outputs": "{ impacted_symbols[], impacted_files[], impacted_tests[], directness_ranked[], depth_reached }"
    },
    {
      "name": "explain",
      "category": "core",
      "tier": "both",
      "summary": "Assemble a grounded, citation-backed context bundle for a symbol/file/change \u2014 definition + neighbors + rationale comments + covering tests + recent history \u2014 pre-formatted for an LLM to reason over. The 'give me everything an agent needs, cited' op. Reuses Pulse BuildReviewContext-style assembly.",
      "inputs": "target (symbol|file|node id) OR changed_paths[]; repo_id (optional); depth (default 2); include_history (bool, default false); budget_tokens (optional cap)",
      "outputs": "{ summary, citations:[{repo_id, path, line, snippet, kind}], neighbors[], tests[], rationale_notes[], history? }"
    },
    {
      "name": "graph_export",
      "category": "core",
      "tier": "both",
      "summary": "Export the knowledge graph (or a subgraph) as a portable graph.json \u2014 graphify-compatible schema plus Atlas extensions (repo_id, snapshot_id, route edges, coverage edges). The interop/offline-analysis op. Mirrors graphify graph.json.",
      "inputs": "repo_id (optional; all linked if omitted); snapshot_id (optional, defaults latest); scope (full|subgraph); root (node id, required if subgraph); depth (subgraph only); format (graphjson|jsonl); include_source (bool, default false)",
      "outputs": "{ nodes:[...], edges:[...], meta:{repo_id, snapshot_id, commit_sha, schema_version} } (streamed file/blob)"
    },
    {
      "name": "cross_repo_impact",
      "category": "cross-repo",
      "tier": "hosted",
      "summary": "Org-wide blast radius: from a change in one repo, find impacted symbols/tests in OTHER repos by matching HTTP route contracts (producer route -> consumer call site) and shared interfaces. The flagship moat graphify structurally lacks. Ports CodeIntelligenceService.CrossRepoImpactForChangedPaths.",
      "inputs": "repo_id; changed_paths[] OR symbols[] OR diff; max_depth (default 2); include_tests (bool, default true)",
      "outputs": "{ origin_repo, impacted:[{repo_id, symbol, path, line, via: route_contract|interface|import, contract:{method,path}, confidence}], impacted_tests_by_repo{} }"
    },
    {
      "name": "consumers",
      "category": "cross-repo",
      "tier": "hosted",
      "summary": "Reverse cross-repo lookup: which repos/services CONSUME a given producer endpoint, interface, or symbol (who depends on me). Powers 'safe to change this API?' Built on the cross-service route-contract analyzer.",
      "inputs": "repo_id; target (route {method,path} | symbol | interface); direction (consumers|producers, default consumers)",
      "outputs": "{ target, matches:[{repo_id, service, path, line, call_kind, contract:{method,path}, confidence}] }"
    },
    {
      "name": "route_contracts",
      "category": "cross-repo",
      "tier": "hosted",
      "summary": "List the resolved HTTP route contracts for a repo/org: producer routes (handlers) and consumer call sites, with their match state. The introspection op behind cross_repo_impact/consumers. Ports route_contract_analyzer + cross_service_analyzer output.",
      "inputs": "repo_id (optional; org-wide if omitted); role (producer|consumer|all, default all); method (optional); path_glob (optional); matched_only (bool, default false)",
      "outputs": "{ contracts:[{role, method, path, repo_id, symbol, file, line, matched_to[], confidence}] }"
    },
    {
      "name": "history",
      "category": "temporal",
      "tier": "both",
      "summary": "Temporal history for a symbol/file/repo: the chain of per-commit snapshots that touched it \u2014 when it changed, in which commit, and the delta. graphify is point-in-time; Atlas is time-aware. Built on per-commit RepoSnapshot + delta indexing.",
      "inputs": "target (symbol|file|repo_id); repo_id (optional); since (commit|date, optional); until (optional); limit (default 50)",
      "outputs": "{ target, entries:[{snapshot_id, commit_sha, authored_at, change:added|modified|removed, delta_summary}] }"
    },
    {
      "name": "snapshot_diff",
      "category": "temporal",
      "tier": "both",
      "summary": "Structural diff between two snapshots (commits) of a repo: symbols/edges/routes added, modified, removed \u2014 graph-level, not text-level. Powers 'what really changed semantically between v1 and v2'. Built on snapshot + delta machinery.",
      "inputs": "repo_id; from (snapshot_id|commit); to (snapshot_id|commit, default latest); scope (symbols|edges|routes|all, default all)",
      "outputs": "{ from, to, added:{symbols[],edges[],routes[]}, modified:{...}, removed:{...}, counts{} }"
    },
    {
      "name": "coverage",
      "category": "test-intel",
      "tier": "both",
      "summary": "The symbol<->test coverage map: which tests cover a given symbol/file, or which symbols a given test exercises. The substrate tests_for_change is built on; also answers coverage-gap queries. Uses CodeSearchService coverage edges (coverage_type ut|e2e|integration).",
      "inputs": "target (symbol|file|test_id); repo_id (optional); direction (tests_for_symbol|symbols_for_test, default tests_for_symbol); coverage_types[] (optional)",
      "outputs": "{ target, coverage:[{test_id|symbol, path, line, coverage_type, strength}], covered (bool), gap (bool) }"
    },
    {
      "name": "status",
      "category": "admin",
      "tier": "both",
      "summary": "Health + indexing state of the engine: store driver in use, vector backend (if any), per-repo index freshness, queue depth, last snapshot per repo. The 'is it ready / what's stale' op for CLI, daemon, and monitoring.",
      "inputs": "repo_id (optional; engine-wide if omitted); verbose (bool)",
      "outputs": "{ tier, storage_driver, vector_backend|null, repos_indexed, queue:{pending,running,failed}, repos:[{repo_id, last_snapshot, commit_sha, indexed_at, stale}] }"
    },
    {
      "name": "repos",
      "category": "admin",
      "tier": "both",
      "summary": "List the repositories known to the engine and their index/snapshot metadata. Local tier: the folders indexed on this machine; hosted tier: the org's linked repos. Ports RepoIndex/RepoIndexJob listing.",
      "inputs": "filter (optional name glob); include_stats (bool, default true)",
      "outputs": "{ repos:[{repo_id, name, path|remote, default_branch, snapshots, symbols, last_indexed, language_breakdown}] }"
    },
    {
      "name": "link",
      "category": "admin",
      "tier": "hosted",
      "summary": "Register/link a repository (and its git webhook) into the org-wide hosted graph so it participates in cross-repo impact, durable indexing queue, and webhook-driven delta reindexing. The hosted-tier onboarding op; local tier resolves this to a no-op/path-add. Backed by RepoIndexJob + webhook wiring.",
      "inputs": "repo (remote url|org/name OR local path); default_branch (optional); webhook (bool, default true on hosted); index_now (bool, default true)",
      "outputs": "{ repo_id, linked:bool, webhook_registered:bool, initial_job_id, status }"
    },
    {
      "name": "semantic_search",
      "summary": "OPTIONAL, gated vector/semantic search over symbol embeddings \u2014 only when VectorStore is enabled (off by default). Atlas core stays lexical; this is the opt-in semantic layer, and Pulse (the LLM layer) is its primary consumer.",
      "inputs": "query; repo_id (optional); limit; min_score",
      "outputs": "{ results, degraded }",
      "tier": "both",
      "category": "core"
    },
    {
      "name": "job",
      "summary": "Indexing-job lifecycle: poll status / stream progress for a long-running index/reindex. Deterministic, index-only \u2014 NOT the agentic run lifecycle, which lives in Pulse.",
      "inputs": "job_id; action (get|events)",
      "outputs": "{ job_id, status, stage, progress }",
      "tier": "both",
      "category": "admin"
    }
  ]
}